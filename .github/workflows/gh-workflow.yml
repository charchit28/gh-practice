# This is a basic workflow to help you get started with Actions

name: Basic workflow
on: workflow_dispatch
jobs:
 first-job:
   runs-on: ubuntu-latest
   steps:
     - name: print-greeting
       run: echo "Welcome to first example"
     - name: print-goodbye
       run: echo "Done bye"
   
